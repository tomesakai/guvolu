# guvolu · 开发铁律与规范集

> 本文件是本仓库的**最高约束**，优先级高于任何临时判断。
> 冲突时：**铁律 T > 风控 R > 数据 D > 其余规范 > 个人偏好**。
> 每条规则带编号，代码注释、PR、Issue 中一律用编号引用（如 `# T-05`）。
> 本文件**必须入库**，不得进 `.gitignore`。
>
> 编号域与文档清单：[docs/00-rules-registry.md](docs/00-rules-registry.md)
> 事实依据：[docs/2026-08-05-gmo-api-capability-report.md](docs/2026-08-05-gmo-api-capability-report.md)
> 架构与未决项：[docs/architecture.md](docs/architecture.md)

## 0. 项目定义

在 GMO Coin API 之上构建量化自动交易体系，含研究（CUDA 因子生成）、执行、控制面三部分。

**已决策**（2026-08-05）：

| 项 | 决策 |
|---|---|
| 交易范围 | **先现物**，架构预留杠杆分支（见 T-09） |
| 策略形态 | 网格、趋势、做市**都要做**，先建基础设施 |
| 研究管线 | 后续接入基于 CUDA 的因子生成（见 G 章） |
| 风控限额 | 先保守，UI 可调但不得突破配置硬顶（见 T-11） |
| 运行形态 | **WS 实时 + 定时全量对账，两者都要**（见 C-10、R-08） |
| 资金定位 | 当前 3,009 JPY 为**验证资金**，目标是链路正确性，不是收益 |
| 实施顺序 | **交易管线（阶段 1 至 8）先行**；CUDA 研究管线（阶段 9）完全独立，后行接入（2026-08-05 决策） |

两把密钥能力**完全正交**，这是实测确认的硬约束：

| 密钥 | 可执行 | 不可执行 |
|---|---|---|
| `READ_ONLY` | 13 个 REST 读取 + 4 个 Private WS 频道 | 任何写操作 |
| `TRADE` | 下单/改单/撤单（5 个） | **任何读取**（含自身挂单） |

## 1. 铁律（T）· 不可协商

| 编号 | 铁律 | 违反后果 |
|---|---|---|
| **T-01** | 密钥只从环境变量读取。**绝不**写进代码、日志、报错、测试夹具、UI、提交信息。日志中一律脱敏为 `sk_***`。 | 资金失窃 |
| **T-02** | `READ_ONLY` 绝不用于写路径；`TRADE` 绝不用于读路径。两把密钥由**不同的 client 类**持有，类型层面不可互换。 | 权限边界形同虚设 |
| **T-03** | **唯一真相源是 `READ_ONLY`。** `TRADE` 的 HTTP 响应只证明「请求被受理」，不证明「状态已改变」。持仓、挂单、成交一律以 READ_ONLY 为准。 | 持仓误判，重复下单 |
| **T-04** | 缺省模式为模拟运行（dry-run）。切换实盘（live）必须显式设置环境变量**且**在启动日志与 UI 中醒目标示。绝不把实盘写成代码默认值。 | 误触实盘 |
| **T-05** | 每一笔下单前必须先落盘自生成的 `intent_id`（全局唯一）；发送成功后立即持久化交易所 `orderId` 与 `intent_id` 的映射，两把密钥之间关联键为 `orderId`（GMO 无客户端自定义委托号，2026-08-05 勘误核实）。同一品种同一时刻至多一笔在途写请求，保证超时对账唯一匹配。 | 无法对账 |
| **T-06** | 写请求超时或网络失败时：**先用 READ_ONLY 查询实际状态，再决定下一步。绝不盲目重发。** | 重复下单 |
| **T-07** | 紧急停止开关（`cancelBulkOrder` 全撤）必须能**脱离主策略进程**独立执行——独立脚本、独立入口、不依赖策略状态与 UI。 | 异常时无法处置 |
| **T-08** | **金额域**（价格、数量、余额、手续费、盈亏）一律 `Decimal`，禁止 `float`。API 返回的字符串直接进 `Decimal`，不经过 `float`。适用边界见 **G-05**。 | 精度损失，下单被拒 |
| **T-09** | 现阶段**代码级硬性禁止杠杆品种**（`*_JPY` 形态 symbol），白名单放行而非黑名单拦截。杠杆分支可在类型系统中预留，但执行路径必须在 `closeOrder`/`closeBulkOrder` 权限开通前保持不可达，且由单测守护。 | 可开仓而不可平仓 |
| **T-10** | HTTP 200 不等于成功。**必须判 `status == 0`**，否则按 `messages[0].message_code` 处理。 | 静默失败 |
| **T-11** | 单笔金额、单日累计下单额、单日最大下单笔数三项限额**必须存在**，超限**直接熔断退出**而非警告。UI 可在运行时调整，但**只能调低**，绝不可突破配置文件中的绝对硬顶（`MAX_*_CEILING`）。硬顶只能改配置并重启。 | 失控亏损 |
| **T-12** | 策略上实盘前必须依次通过：回测、模拟运行纸上交易、最小手数实盘。**不得跳级。** | 以实盘资金调试 |
| **T-13** | 研究与回测进程**永不持有 `TRADE` 密钥**。进程级隔离，不是代码级约定。 | 研究代码误下单 |

**起步限额**（保守档，可在 UI 内下调）：单笔 ≤ 500 JPY，单日累计 ≤ 2,000 JPY，单日 ≤ 50 笔。
**绝对硬顶**：单笔 ≤ 1,000 JPY，单日累计 ≤ 3,000 JPY。

## 2. 风控原则（R）

- **R-01｜先有出口再有入口**：任何建仓逻辑必须在下单**之前**确定退出条件（止损价、止盈价、最长持仓时间）。无退出条件的入场是缺陷。
- **R-02｜熔断优先于收益**：连续 N 次异常、余额异动超阈值、行情断流超时——先停止交易并全撤，事后再排查。
- **R-03｜服务状态门禁**：下单前检查 `GET /v1/status` 为 `OPEN`。`MAINTENANCE` 与 `PREOPEN` 期间不发任何写请求。
- **R-04｜限速留余量**：tier1 上限 20 req/s，代码按 **10 req/s** 自我约束。WS 订阅严格 1 req/s。
- **R-05｜时钟即安全**：`API-TIMESTAMP` 用毫秒 UNIX 时间。本机时钟偏移导致全部签名失败；启动时校验偏移，超过 ±5s 拒绝启动。
- **R-06｜资金规模自觉**：当前余额 3,009 JPY，BTC 现物最小委托约 101 JPY。本阶段目标是**验证链路与正确性**。
- **R-07｜每一笔实盘写操作留痕**：请求、响应、时间戳、`correlation_id` 与 `intent_id` 全量落盘，可事后完整重建（GMO 无客户端自定义委托号，2026-08-05 勘误，关联键为交易所 `orderId`，见 T-05）。
- **R-08｜双通道对账**：WS 实时回报与定时 REST 全量快照**必须相互校验**。两者不一致时以 REST 为准，并记为一次异常事件计入 R-02 的熔断计数。

## 3. 代码原则（C）

### 3.1 语言与工程

- **C-01**：Python 3.12+，全量类型注解，`mypy --strict` 通过。
- **C-02**：策略层写成**纯函数**（输入行情与持仓，输出意图），所有 IO 收敛到边界层。策略必须可在无网络下单测。
- **C-03**：禁止以 `except Exception:` 统一捕获并忽略异常。按具体异常处理，无法处理即中止进程——中止优于带异常状态下单。
- **C-04**：配置一律来自环境变量或配置文件，**零硬编码**。魔数必须具名常量。
- **C-05**：依赖锁定版本。不引入非必要依赖。

### 3.2 API 层

- **C-06**：签名串 = `timestamp + method + path + body`；`path` 以 `/v1` 开头（**不含** `/private`）；GET 的 body 为空串，query 不参与签名。
- **C-07**：**例外**——`PUT` / `DELETE /v1/ws-auth` 的签名串**不含 body**（body 照发）。GMO 的不一致设计，代码中必须显式注释。
- **C-08**：自动重试**只对 GET**（幂等），指数退避加随机抖动。**写请求永不自动重试**（见 T-06）。
- **C-09**：WS 订阅后必须解析返回帧中的 `{"error": ...}`。权限错误以消息返回**且不断开连接**，不处理将持续收不到数据且无法察觉。
- **C-10**：WS 必须处理 60s 心跳与自动重连；重连后**必须用 REST 拉取全量快照对账**（R-08），不得假设增量连续。
- **C-11**：时间内部统一 UTC，仅展示层转 JST。GMO 交易日边界为 **JST 06:00**，`klines` 的 `date` 参数按 JST。
- **C-12**：4 个历史查询端点必传 `fromTimestamp`（ISO8601），否则 `ERR-5106`。

### 3.3 测试

- **C-13**：单元测试禁止访问网络。API 层用**录制回放**（真实响应存为夹具）。
- **C-14**：**测试绝不打真实写端点。** 验证写路径权限时使用故意非法参数的探测法（见调查报告方法节）。
- **C-15**：签名逻辑、`Decimal` 换算、状态机迁移、point-in-time 正确性必须有直接单测覆盖。

## 4. 数据架构原则（D）

> 目标：**无歧义**。任何一个数字，都能回答「它从哪来、在哪个时刻可见、由哪版代码产生」。
> 具体选型（存储引擎、文件格式、表结构）见 [docs/architecture.md](docs/architecture.md) 的 TBD 台账。

- **D-01｜单向血缘**：数据严格单向流经 `raw → normalized → feature → signal → intent → order → execution → pnl`。每层只依赖上一层，**禁止跨层依赖、禁止回写上游**。
- **D-02｜raw 层不可变**：原始 API 响应原样落盘，仅追加，**永不覆写、永不回填、永不删除**。修正只能在下游层以新记录表达。
- **D-03｜时间四元组分开存储**：`event_time`（经济事件发生时刻）、`available_time`（最早可合法得知时刻）、`ingest_time`（本地落盘时刻）、`decision_time`（策略实际使用时刻）。回补历史时 `ingest_time` 是下载时刻，不承担防未来函数职责；同一事件的修订以 `revision_id` 生成新记录表达（配合 D-02）。**时间混用是未来函数的主要来源。**
- **D-04｜point-in-time 正确性**：任何特征在 `t` 时刻只能看到 `available_time <= t` 的数据。回测与实盘**共用同一份特征代码**，仅数据源不同。违反此条的因子一律不得进入策略。
- **D-05｜标识符全局唯一且稳定**：`intent_id` / `order_id` / `execution_id` / `position_id` / `feature_id` / `run_id` / `strategy_version` / `correlation_id`。一经生成不得复用、不得变更语义。
- **D-06｜schema 版本化**：每份持久化数据带 `schema_version`。字段**只增不改语义**；语义变化必须新增字段并升版本。
- **D-07｜金额不落浮点**：存储层用字符串或最小单位整数表示金额，**禁止浮点存储**。读出即转 `Decimal`。
- **D-08｜时间统一 UTC 存储**；交易日归属必须显式建模为 `trading_day` 字段（边界 JST 06:00），不得在查询时临时推算。
- **D-09｜产物可追溯**：每份 feature、signal、回测结果必须记录产生它的 `code_version`（git hash）、`config_hash`、`run_id`。无法追溯的产物不得用于决策。
- **D-10｜回测与实盘同源**：同一策略函数、同一特征代码、同一风控代码。若两条路径出现分叉实现，视为架构缺陷。

## 5. 控制面 / UI-UX 原则（X）

> 目标：UI 让人**看得懂、按不错**。具体框架与页面设计见 [docs/architecture.md](docs/architecture.md) TBD 台账。

- **X-01｜UI 是控制面，不是数据源**：UI 不得成为任何状态的唯一持有者。关闭 UI，交易与风控必须照常运行。
- **X-02｜危险操作三态可见**：`查看` / `模拟运行` / `实盘` 三态在界面上始终可辨。切到实盘必须二次确认，并明示**将触碰哪些端点**。
- **X-03｜紧急停止开关常驻**：任何页面一键可达，视觉上最高优先级，且**不经过策略进程**（T-07）。
- **X-04｜金额必须限定语义**：显示时明确区分 `amount`（总额）与 `available`（可用），禁止笼统显示「余额」（U-03）。
- **X-05｜限额只能调低**：UI 的限额调整受配置硬顶约束（T-11），界面上同时显示「当前值 / 硬顶」。
- **X-06｜陈旧数据必须显式标记**：所有实时数字带更新时刻与陈旧度指示。断流时显示为「陈旧」，**绝不静默显示旧值**。
- **X-07｜术语一致**：UI 文案严格使用第 7 章术语表的中文列，不得自创同义词。
- **X-08｜可下钻解释**：任一持仓或挂单可回溯到产生它的 `intent → signal → feature → raw`（D-01 血缘链）。
- **X-09｜研究界面与交易界面分离**：因子研究与回测视图不得与实盘操作按钮同屏，避免误触。

## 6. 研究与 GPU 管线原则（G）

> 后续接入。此处仅锁定不可动摇的边界，实现细节见 TBD 台账。

- **G-01｜进程隔离**：研究管线独立进程或独立容器，**永不持有 `TRADE` 密钥**（T-13）。
- **G-02｜只读上游**：因子层只读 `raw` 与 `normalized`，**不得回写**（D-01）。
- **G-03｜可复现**：固定随机种子，记录 CUDA、驱动、库版本与 `code_version`。不可复现的结果不得进入策略。
- **G-04｜因子准入**：任何因子进入策略前必须通过 point-in-time 检验（D-04）与样本外验证。
- **G-05｜float 的适用边界（T-08 的例外与闸门）**：
  - **数值研究域**（因子计算、统计、模型训练与推理）：允许 `float32` / `float64`，GPU 计算本就如此。
  - **金额域**（价格、数量、余额、下单参数）：**必须 `Decimal`**。
  - **闸门**：因子输出转为下单参数时，必须经过唯一一处显式转换函数完成 `float → Decimal` 并按 `tickSize` / `sizeStep` 取整。**该转换点是全项目唯一允许两域相接的位置**，必须有单测覆盖。
- **G-06｜阈值即配置**：研究与评估中的一切筛选阈值、预算、配比均为版本化配置，随评估记录留存；不得在文档或结论中表述为事实或普适结论。
- **G-07｜试验台账全量登记**：每个候选的评估事实（含静态闸门被拒者）以 `candidate_id` 与 `evaluation_id` 追加式登记于试验台账，永不删除；多重检验校正必须使用全量试验计数。
- **G-08｜封存段一次消费**：样本外封存段仅允许消费一次，消费后永久标记；检验失败后修改产生的新版本不得复用同段声称样本外。从未开始评估的封存段可显式废弃并留痕，新段起点须晚于废弃时刻。

## 7. 术语统一表（用语铁律）

**同一概念在代码、注释、文档、UI、对话中只用一种说法。** 中文交流用「中文」列，标识符用「代码标识符」列。

| 日语（官方） | English | 中文 | 代码标识符 |
|---|---|---|---|
| 注文 | order | 委托 | `order` |
| 有効注文 | active order | 挂单 | `active_order` |
| 約定 | execution / fill | 成交 | `execution` |
| 建玉 | position | 持仓 | `position` |
| 建玉サマリー | position summary | 持仓汇总 | `position_summary` |
| 決済注文 | close order | 平仓委托 | `close_order` |
| 余力 | available margin | 可用余力 | `margin` |
| 資産残高 | assets | 资产余额 | `assets` |
| 現物 | spot | 现物 | `spot` |
| レバレッジ | leverage / margin | 杠杆 | `leverage` |
| ロスカット | losscut | 强制平仓 | `losscut` |
| 銘柄 | symbol | 品种 | `symbol` |
| 板 | orderbook | 盘口 | `orderbook` |
| 最良売気配 / 最良買気配 | best ask / best bid | 最优卖价 / 最优买价 | `best_ask` / `best_bid` |
| 取引履歴 | trades | 逐笔成交 | `trades` |
| 数量 | size | 数量 | `size` |
| 指値 / 成行 / 逆指値 | LIMIT / MARKET / STOP | 限价 / 市价 / 止损 | `LIMIT` / `MARKET` / `STOP` |
| 取引高 | trading volume | 交易额 | `trading_volume` |
| 口座振替 | transfer | 账户划转 | `transfer` |

以下为本项目通用工程术语（非 GMO 官方用语），按 W-05 登记：

| 中文 | English | 日本語 | 定义 |
|---|---|---|---|
| 紧急停止开关（kill-switch） | kill switch | 緊急停止 | 独立于策略进程的全量撤单入口（T-07） |
| 模拟运行（dry-run） | dry run | ドライラン | 不发送真实写请求的运行模式 |
| 实盘（live） | live | 本番 | 发送真实写请求的运行模式 |
| 未来函数 | look-ahead bias | 先読みバイアス | 特征使用了决策时刻不可见的数据 |
| 熔断 | circuit breaker | サーキットブレーカー | 触发条件满足时停止交易并全撤的机制 |
| 合规循环 | compliance loop | コンプライアンスループ | 以自动测试反复校验文档与代码合规的机制 |
| 校验框架（harness） | test harness | テストハーネス | 承载并执行合规校验的程序 |
| 录制回放 | record and replay | 記録再生 | 以真实响应存档驱动测试的方法 |
| 未决项（TBD） | to be determined | 未定事項 | 登记于台账、尚未确定的设计项 |
| 表情符号（emoji） | emoji | 絵文字 | 图形化符号字符，文档中禁用（W-06） |
| 因子挖掘 | factor mining | ファクターマイニング | 批量生成并统计筛选因子的研究过程 |
| 候选 | candidate | 候補 | 因子表达式与参数的具体组合 |
| 试验台账 | trial ledger | 試行台帳 | 追加式记录全部候选评估事实的台账 |
| 封存段 | holdout vintage | ホールドアウト区間 | 仅允许消费一次的样本外数据段 |
| 冻结前向预测 | frozen forward prediction | 凍結フォワード予測 | 封存段开始前固定候选与权重，并在区间内逐决策时点不可改写地追加的预测 |
| 缺预测处置政策 | missing policy | 欠測処置ポリシー | 冻结计划封存前预登记的缺预测柱处置方式，取 `burn` 或 `zero_exposure`，不得事后增补 |
| 制品 | artifact | 成果物 | 以内容散列标识的持久化产物文件 |
| 内容寻址 | content addressing | コンテンツアドレス方式 | 以内容散列作为身份与检索键 |
| 回放游标 | replay cursor | リプレイカーソル | 跨视图同步回放的时点标识 |
| 面板 | panel | パネル | 固定品种、字段与期间的研究数据矩阵 |
| 来源 | venue | データ提供元 | 产生数据的交易所或数据供应商 |
| 规范化品种 | canonical instrument | 正規化銘柄 | 跨来源统一命名的交易对象标识 |
| 来源优先级 | source priority | ソース優先順位 | 同一数据域内主来源与备用来源的固定次序 |
| 参考价 | reference price | 参照価格 | 多来源合成的价格序列，只用于研究与监控 |
| 一致性裁决 | reconciliation ruling | 整合判定 | 多来源对同一事实不一致时选定采信值的规则 |
| 完整性等级 | integrity class | 完全性クラス | 流数据无缺失可证明程度的分级 |
| 合成标识 | synthetic identifier | 合成識別子 | 来源无原生标识时按内容确定性生成的替代标识 |
| 足迹图 | footprint chart | フットプリント | 每根 K 线展开为价格档乘主动方向量矩阵的图型 |
| Delta | delta | デルタ | 每 bar 主动买量减主动卖量（吃单方向口径） |
| 累计 Delta | cumulative volume delta | 累積デルタ | Delta 的会话内累计，跨交易日断开 |
| 控制价位（POC） | point of control | ポイントオブコントロール | bar 或会话内总成交量最大的价格档 |
| 裸 POC | naked POC | 未回帰 POC | 后续会话尚未被触及的既往会话控制价位 |
| 价值区（VA） | value area | バリューエリア | 以 POC 为中心覆盖总量七成的档带，边界 VAH 与 VAL |
| 失衡 | imbalance | インバランス | 对角买卖量比超阈的档；连续多档为堆叠失衡 |
| 吸收 | absorption | アブソープション | 大量主动成交打入而价格无进展的形态判定 |
| 衰竭 | exhaustion | 枯渇 | bar 极值端顺势主动量显著萎缩的形态判定 |
| 会话剖面 | session profile | セッションプロファイル | 单交易日的价格量分布直方图 |
| 高量节点与低量节点 | HVN and LVN | 高出来高ノードと低出来高ノード | 量分布中的局部峰档带与谷档带 |
| 阻力墙 | order wall | 板の壁 | 挂量分位与持续时长双超阈的单侧档（含支撑墙） |
| 磁力区 | liquidity magnet | 流動性マグネット | 时间积分挂量密度的高值价格带 |
| 挂单欺诈嫌疑 | spoofing suspicion | 見せ板疑い | 大额挂单短时撤除且成交不足的形态嫌疑判定 |
| 撮合口径 | per-match basis | 約定ベース | 每笔撮合计量一次的量口径（归档口径） |
| 双侧计量 | double-sided counting | 両側計上 | 每笔撮合按买卖两行合计的量口径（官方 K 线口径） |
| 细节度（LOD） | level of detail | 詳細度 | 按 bar 像素宽切换渲染密度的分级机制 |
| 订单流失衡（OFI） | order flow imbalance | オーダーフロー不均衡 | 最优档挂量增量的有向累计指标 |
| 盘口不平衡 | book imbalance | 板の偏り | 带内买卖挂量之差与合计之比 |
| 微价格 | microprice | マイクロプライス | 以最优买卖价和对侧最优档数量加权的短时价格参考 |
| 固定金额冲击 | fixed-notional impact | 定額インパクト | 指定计价金额穿透当前盘口所得的 VWAP 相对最优报价偏离 |
| 净增挂 | net add | 純増し板 | 桶内档位挂量净增部分（增撤相抵后） |
| 净撤减 | net cancel | 純減し板 | 桶内档位挂量净减部分，含移仓不可分辨 |
| 成交消耗 | executed depth | 約定消化 | 桶内档位被逐笔成交吃掉的量 |
| 补单 | replenishment | 補充注文 | 成交消耗后同档挂量的回补 |
| 存续期 | lifetime | 存続期間 | 档位挂量自首见至消失的时长 |
| 撤单率 | cancel ratio | キャンセル率 | 窗口内净撤减与净增挂之比 |
| 扫单 | sweep | スイープ | 单侧连续多档在短时内被成交击穿 |
| 抽离 | pull | 引き板 | 价格逼近时无成交的大幅净撤减 |
| 流动性真空 | liquidity vacuum | 流動性真空 | 带内挂量跌破低分位的稀薄状态 |
| 瓦片金字塔 | tile pyramid | タイルピラミッド | 多缩放级预聚合网格文件的分层组织 |
| 订单流 | order flow | オーダーフロー | 逐笔与盘口事件合并观测的研究域与页面名 |
| 当前盘口列 | book column | 現在板カラム | 热力图右缘与价格轴对齐的迷你梯形 |
| 成交刻线 | print tick | 約定ティック | 大额成交在其价位的定长横刻线标注 |
| 导航条 | navigator | ナビゲーターバー | 覆盖范围与视窗框的缩略操作细条 |
| 报警 | alert | アラート | 判读信号匹配规则实例后的触发通知 |
| 确认（报警） | acknowledge | 確認 | 将触发态报警标记为已阅的本地操作 |
| 跟随模式 | follow mode | 追従モード | 盘口梯形随刷新保持中间价居中的缺省态 |
| 决策输入（DecisionInput） | decision input | 決定入力 | 每根决策柱冻结并内容寻址的预测输入束，含身份、输入收据、质量、调节输入与代码身份 |
| 决策记录（DecisionRecord） | decision record | 決定記録 | 由决策输入产生、带有效期与目标域声明的不可变预测制品 |
| 执行目标（ExecutionTarget） | execution target | 執行目標 | 适配器由决策记录生成、仅供执行域消费的暴露目标快照 |
| 搜索束（SearchBundle） | search bundle | 探索バンドル | GPU 搜索的内容寻址只读输入集合，含面板张量、掩码、搜索计划与容差身份 |
| 三态逻辑 | three-valued logic | 三値論理 | 布尔运算含真、假与未知三种取值，未知表示缺失或不可判定 |
| 意图账本 | intent ledger | インテント台帳 | 下单意图与其状态迁移的追加式持久化记录（T-05、R-07） |

### 用语铁律

- **U-01｜委托 ≠ 成交**：`order` 是意图被交易所受理，`execution` 是实际成交。一笔 `order` 对应 0..N 笔 `execution`。混用视为缺陷。
- **U-02｜现物与杠杆 symbol 形态不同**：现物 `BTC`，杠杆 `BTC_JPY`。**必须由类型区分**（`SpotSymbol` / `LeverageSymbol`），不得用裸字符串传递。
- **U-03｜「余额」必须限定**：`amount`（总额）与 `available`（可用）语义不同，禁止笼统称「余额」。
- **U-04｜方向词统一**：一律 `BUY` / `SELL`，不用「多/空」描述现物。
- **U-05｜英文缩写不自创**：沿用官方字段名，不发明 `qty`、`px`、`amt`。
- **U-06｜中文文档保留官方日语端点名**，便于与官方文档对照，如「建玉一覧を取得（`/v1/openPositions`）」。
- **U-07｜因子、信号、意图三者不可混称**：`factor` 是数值特征，`signal` 是策略判断，`intent` 是待执行的下单意图。三者是 D-01 血缘中的不同层。

## 8. 目录与命名约定

```text
guvolu/
├── SKILLS.md                    # 本文件（入库）
├── CLAUDE.md                    # 入口索引
├── .gitignore                   # .env 必须在内
├── .env / .env.example          # .env 永不入库
├── docs/
│   ├── 00-rules-registry.md     # 0 号文档（W-01）
│   ├── architecture.md          # 架构与 TBD 台账
│   ├── error-catalog.md         # 错误码对照与处置册
│   └── 2026-08-05-gmo-api-capability-report.md   # 时效快照
├── src/guvolu/
│   ├── api/                     # 边界层：签名、HTTP、WS
│   │   ├── read_client.py       # 只持有 READ_ONLY（T-02）
│   │   ├── trade_client.py      # 只持有 TRADE（T-02）
│   │   ├── public_client.py     # 无需密钥
│   │   └── ws_private.py
│   ├── domain/                  # 纯数据模型，Decimal
│   ├── data/                    # 血缘各层的读写（D 章）
│   ├── research/                # 因子与 GPU 管线（G 章），无 TRADE 密钥
│   ├── strategy/                # 纯函数（C-02）
│   ├── risk/                    # 熔断与限额（T-11）
│   ├── execution/               # 执行链发送编排（T-05、T-06）
│   ├── ui/                      # 控制面（X 章）
│   └── ops/
│       └── kill_switch.py       # 独立入口（T-07）
├── web/                         # 查看版前端（TBD-12 已锁定栈）
├── scripts/                     # 一次性探测与运维脚本
└── tests/
    └── test_doc_compliance.py   # 合规循环（W-03）
```

**命名**：模块与函数 `snake_case`，类 `PascalCase`，常量 `UPPER_SNAKE`。文件名不用缩写。测试文件 `test_<被测模块>.py`。

**提交信息**：`<type>(<scope>): <中文摘要>`，type ∈ `feat|fix|docs|refactor|test|chore`。涉及规则时在正文引用编号，如 `遵循 T-06：写请求超时改为查询后决策`。

## 9. AI 代理协作规则（A）

> 本项目预期由 AI 代理调用与维护。本章界定**自主执行的边界**：边界之内自主决策并执行，边界之外需人工确认。设计意图是使代理能独立完成研究、开发、模拟运行与对账，而非逐步审批。
> 边界之外仅三类，均为资金安全不变量，其余一律视为边界之内。

- **A-01｜自主边界**：代理可自主完成读取、研究、回测、模拟运行、对账，以及代码与文档的修改。仅以下三类需人工确认：切换实盘（T-04）、调整 T-11 硬顶、变更密钥权限配置。
- **A-02｜实盘与硬顶不自行放宽**：不得自行将模拟运行改为实盘，不得自行放宽 T-11 的限额或硬顶。此为安全不变量，非流程环节。
- **A-03｜写操作可预期**：产生真实写请求的改动，在提交信息或变更说明中列明所触碰的端点。此为记录义务，不构成执行前的审批环节。
- **A-04｜结论可溯源**：API 行为结论来自实测或官方文档并注明依据。存疑时以探测法验证（C-14），不依赖记忆——本项目调查中两份文档摘要曾给出互相矛盾的路径。
- **A-05｜未决项登记**：遇到本文件未覆盖的设计抉择，登记到 [docs/architecture.md](docs/architecture.md) 台账。代理可提出并实施提案，在台账中标注为提案并说明理由即可；已标注【已锁定】的条目不得单方变更。
- **A-06｜密钥不外泄**：不得将密钥内容写入任何文件、输出、日志或提交。

## 10. 文书与注释规范（W）

- **W-01｜编号统一登记**：所有编号域（含 TBD）由 0 号文档 [docs/00-rules-registry.md](docs/00-rules-registry.md) 统一登记。新增、废弃编号必须同步该册；编号不复用，废弃规则原地标注「已废弃」并保留。
- **W-02｜文档分类与命名**：文档分「长期维护」与「时效快照」两类，均须登记于 0 号文档清单。时效性调查结果为时效快照，命名 `YYYY-MM-DD-主题.md`，内容冻结，修订以新快照发布。
- **W-03｜合规循环**：内容合规（链接、编号一致性、清单完整性、符号与注释）由 [tests/test_doc_compliance.py](tests/test_doc_compliance.py) 校验；文档样式格式（标题层级、表格、代码围栏、空行与空白）由 [tests/md_style.test.mjs](tests/md_style.test.mjs) 校验。两者提交前均须通过。新的可机检规范应落实为检查项，而非仅写入文档。
- **W-08｜Markdown 样式**：表格用于对照与枚举，不额外添加分割线；章节之间以标题区分，正文中不使用水平分割线；代码围栏须带语言标注，纯文本图示用 `text`；无行尾空白、无制表符、连续空行不超过一行、文件以单个换行结尾。具体判定以 W-03 的样式脚本为准。
- **W-04｜注释规范**：代码注释一律以中文正规书写，单处不超过二十字；纯规则编号引用除外。文档字符串同样以中文书写，不受二十字限制。
- **W-05｜用词规范**：禁用行业隐语、自造词、口语表达与比喻修辞。非惯用名词与新名词须先在第 7 章术语表以中英日对照加定义登记后方可使用；仅单一文档使用的术语可在该文档头部定义。
- **W-06｜符号规范**：禁用表情符号与装饰性符号、重复标点、非中性情感表达；表格判定用「可 / 不可」等中性文字。
- **W-07｜精简与解耦**：同一信息只在唯一位置表达，其余位置以链接引用；文档在信息完整前提下尽可能简短，文档之间不得互相复述内容。

*规则与文档的新增、废弃流程见 [docs/00-rules-registry.md](docs/00-rules-registry.md)。*
