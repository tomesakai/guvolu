# ADR-0003：时序与截面双 GPU 后端

状态：Accepted  
日期：2026-08-05

## 决定

E0/E1/E2 使用时序后端，E3/M 使用完整横截面后端；边界通过显式 materialization。

## 原因

512 线程完整横截面适合 rank/Top-K，但不适合让任意窗口状态驻留寄存器。CC 12.x 资源限制使统一融合模型不成立。

## 后果

编译器输出 stage DAG；性能评估按 stage class，不再给一个通用候选吞吐。
