# 每小时链逐日变慢、无调度轮次告警与恢复（2026-09-21）

> 文档类别：时效快照，登记于 [docs/00-rules-registry.md](00-rules-registry.md)。
> 范围：2026-09-14 至 21 的实盘事实、每小时冻结前向链从 11 分钟退化到 50 分钟的根因、当日处置与代码修复。合同正文在[执行链设计第 14 节](execution-chain-design.md)与[运行时操作第 5 节](runtime-ops.md)，本文只记录当日事实与决定。

## 1. 实盘事实（09-14 至 09-21 09:00 JST）

| 时刻（UTC） | 市场 | 事件 |
|---|---|---|
| 09-18 04:49 至 09:45 | BTC | 目标暴露由 0.08 升至 0.6，两轮被动成交买入合计 0.00073 BTC（12,184,811 与 12,320,293） |
| 09-20 04:50 | ETH | 目标暴露 0.481，被动成交买入 0.0178 ETH 于 404,379 |
| 09-21 00:44 | 账户 | JPY 16,616；BTC 0.00073；ETH 0.0178；实盘报告估值 33,445，较 09-13 基线 33,008 浮盈 437 JPY（1.3%） |

实盘账本累计 10 笔委托意图；周日成本汇总（09-19）统计 7 笔成交、名义 18,012 JPY，综合成本 5.1 bp。信封 `faceb7f3d3c4`（第十一封）未熔断，有效期至 09-27 00:00Z。

## 2. 现象：轮次变慢直至无完成记录

每小时任务的单轮耗时中位数逐日上升，成功率同步下降：

| 日期 | BTC 中位 / 成功 | ETH 中位 / 成功 |
|---|---|---|
| 09-09 | 15.8 分 / 22 of 22 | 11.3 分 / 22 of 22 |
| 09-14 | 23.2 分 / 24 of 24 | 18.3 分 / 24 of 24 |
| 09-17 | 35.3 分 / 14 of 17 | 25.0 分 / 10 of 19 |
| 09-20 | 37.2 分 / 3 of 13 | 51.0 分 / 1 of 13 |

失败形态依次为 `最新研究柱不在冻结预测登记时效窗口内`、`冻结前向预测未在预登记时效窗口内生成`（预测生成时已过时效）、`活动成交输入收据不是登记时的完整当前 head` 与运行根写锁超时（同一运行根上两轮重叠），以及完全没有完成记录的轮次。观察进程的“无调度轮次”告警来自最后一种：计划任务 55 分钟时限到点只终止 PowerShell 包装脚本，包装脚本来不及写完成记录，日志里该市场超过 130 分钟没有新行。持仓在此期间无人管理，但信封与各门未受影响，未产生错误委托。

## 3. 根因

四个因素互相放大，任何一个单独都不足以拖垮链路：

| 因素 | 事实 | 作用 |
|---|---|---|
| 活动段头只增不减 | 生产补漏运行自 08-26 冻结的运维副本，没有 TBD-40 合并步；BTC 4,457、ETH 4,067 个实时段头（每日约 288 个） | 每轮运行根刷新逐文件校验、冻结预测逐文件拼接，耗时随段数线性上升 |
| 被终止轮次留下孤儿进程 | 任务时限只杀包装脚本，`run_frozen_live.py` 与预测子进程继续运行（09-21 实测 08:30 的 ETH 轮在 09:35 仍存活） | 与下一轮重叠：同一运行根的快照被中途替换、写锁互等、各占约 2 GB 内存 |
| 汇率物化器全程持主写锁 | `fx_materialize watch` 每轮对全部历史段（两万余）逐个读 manifest 并调用物化入口，整轮包在主写锁内；冷缓存下单轮超过 7 分钟，周期 5 分钟 | 主数据根写锁几乎被连续占用，逐笔物化、L2 采集登记与合并任务轮流超时 |
| 内存与磁盘余量 | 质量巡检进程泄漏至 1.9 GB；隐藏控制台经默认终端委托给 Windows Terminal，其私有内存 12 GB；空闲内存一度 0.4 GB，文件缓存被挤空；C 盘余 7%、D 盘余 9%，页面文件 47 GB | 所有依赖元数据扫描的循环退化为磁盘随机读，5.3 GB 控制库单步备份由约 10 秒升至 6 至 50 分钟 |

两个市场相隔 18 分钟起跑；单轮一旦超过 18 分钟即互相争用，此后退化自我加速（09-17 起）。

## 4. 当日处置（09-21 09:10 至 10:30 JST）

| 项 | 处置 | 结果 |
|---|---|---|
| 行情管线 | 依运行时操作第 5 节切到 `ForwardMinimal`（磁盘余量低于 20%）；终止泄漏的质量巡检进程 | L2、book-state、OFL 与质量巡检暂停，六条 raw 采集与实时逐笔物化保留 |
| 段头合并 | 对 BTC 与 ETH 执行 `trade_realtime_compact` | BTC 4,190 个头并为 17 个日分区（139,836 行），ETH 3,837 个并为 17 个（99,247 行）；每轮输入由 4,442 / 4,067 降至 385 / 343；原段制品与 attempt 保留（D-02）。其余五个市场 10:38 至 10:48 由新启动器一次合并完毕（XRP 4,382、SOL 3,720、DOGE 2,446、bitbank 5,177、bitFlyer 4,792 个实时头各降至 44 至 60 个） |
| 汇率物化器 | 禁用 `guvolu-fx-materializer` 任务并终止进程；raw 汇率采集不停 | 主写锁恢复间歇可得；代码修复后重新启用（第 5 节） |
| 孤儿轮次 | 整树终止 08:30 的 ETH 孤儿轮与无望的 09:12 BTC 轮；删除其遗留的 2.8 GB 临时快照 | 09:30 ETH 轮 14.8 分钟完成（此前约 50 分钟），实盘步放行、差分在不交易带内 |

修复部署后的头两轮：BTC 10:12 轮 9.0 分钟、ETH 10:30 轮 6.8 分钟，均完成且实盘步放行（差分在不交易带内，无委托）；运行根刷新分步耗时为备份约 115 秒、完整性检查约 87 秒、输入校验约 1 秒。汇率物化器以修复后的代码重新启用，第二轮起不再持主写锁。

## 5. 代码修复

| 项 | 改动 |
|---|---|
| 包装脚本 `scripts/run_frozen_live_task.ps1` | 轮次开始先写 `phase=started` 记录；Python 以受控子进程运行，自带 50 分钟超时（短于任务 55 分钟时限），超时 `taskkill /T` 整树终止并写 `exit_code=4`、`timed_out=true` 的完成记录；子进程输出固定 UTF-8 |
| 观察进程 `scan_scheduler_health` | 识别开始记录：在时限内的视为运行中并计入最近活动；超时而无完成记录的按未完成轮次计入连续失败，告警写明“轮次超时被终止”，不再表现为“无调度轮次” |
| 每日合并任务 | 新增 `scripts/run_trade_compaction.ps1` 与 `scripts/register_trade_compaction_task.ps1`（`guvolu-trade-compaction`，每日 03:53，七个市场逐个合并，写锁超时逐市场重试三次），当日已登记 |
| 汇率物化器 `fx_materialize._watch` | 扫描移出写锁，只在有待物化段时加锁；已完成段按登记路径跳过、不读 manifest，每 288 轮（约每日）全量复核一次；`all` 与 `audit` 口径不变 |
| 运行根刷新 `scripts/refresh_frozen_runtime.py` | 结果新增 `elapsed_seconds`（等锁、备份、输入、完整性检查分步耗时），每轮随调度日志留痕 |
| 测试 | 包装脚本三例（留痕与透传、超时整树终止含孙进程、运行根不可达）、合并任务定义两例、汇率跳过路径一例、观察进程开始记录用例 |

## 6. 遗留与建议

- 宿主内存：Windows Terminal 进程自 09-11 登录起常驻，私有内存 12 GB，页面文件 47 GB 占用 C 盘。建议维护者在方便时重启一次宿主；全部采集、物化、观察与每小时任务均由登录与守护任务自动恢复。代理未终止该进程，也未改动默认终端设置。
- 磁盘：C 盘余 31 GB（7%）、D 盘余 79 GB（9%）。`ForwardMinimal` 期间派生物化暂停，之后可由 sealed raw 确定性重建；腾出空间前不切回 `Full`。
- 控制库 5.3 GB，每轮整库备份到运行根（两市场每小时合计约 10.6 GB 写入）；分步耗时已留痕，若备份仍是主要成本，再评估两运行根共用同一代快照。
- 第十二封信封 `f6a337900ace`：取值与第十一封完全相同（单笔 15,000、日 45,000 / 20 笔、总额 300,000、持仓 20,000、累计亏损 5,000、日亏 2,500、首单 canary 9,000、BTC 与 ETH），只把有效期顺延为 09-21 02:00Z 至 10-05 00:00Z，避免第十一封 09-27 到期时无人值守而断档。换封即新身份：首单压额重新生效，亏损基线在首轮按当时估值重建。
- BTC 的 `-live` 计划任务登记时重复期为 30 天，到 10-01 09:00 JST 为止（ETH 的到 12-31）；按上膛协议由维护者在此之前用 `scripts/register_frozen_live_task.ps1` 重新登记，结束时刻取 holdout 截止 12-02。

### 6.1 同日补记：磁盘整理与重启准备（12:00 至 12:35 JST）

维护者已重新登记 BTC 的 `-live` 任务（重复期至 12-02 09:00 JST）。磁盘盘点：D 盘 807 GB 中游戏约 510 GB、项目 169 GB；C 盘 420 GB 中项目根 144 GB（`data/materialized` 48 GB、`reports` 24 GB）、页面文件 46 GB。处置只做不丢数据的部分：

| 项 | 处置 | 结果 |
|---|---|---|
| 冷文本透明压缩 | 对 `reports` 下 `*.bin`、`*.jsonl`，`data/materialized` 下 `*.csv`，D 盘 `guvolu-cold-interim/backups` 的旧控制库备份执行 NTFS LZNT1 压缩（`compact /c`，空闲优先级）；逻辑路径、字节与 SHA-256 不变，可用 `compact /u` 还原；不触 Parquet、活动 SQLite 与 `*.jsonl.open` | C 盘 33.9 增至 47.8 GB，D 盘 79.7 增至 83.8 GB；D 盘三所 L2 raw 封口段此前已是压缩态 |
| 下载缓存 | `pip cache purge` 与 `npm cache clean` | 约 3.8 GB |
| 未动 | 回收站、个人文件、游戏、`.codex-tmp`、`search-run.partial-*`、各类模型缓存 | 留给维护者决定 |

重启后页面文件回到系统初始大小，预计 C 盘再回收约 30 GB（约 17%）。要回到 20% 以上并恢复 `Full` 配置，按[运行时操作第 8 节](runtime-ops.md)的既定路径把已暂停、可由 sealed raw 重建的派生 Parquet（C 盘 `book_l2` 25.7 GB、`orderflow_tile` 12.2 GB）迁入 E 盘冷层（E 盘现余 1.8 TB），不另起方案。Windows Terminal 私有内存实测每个隐藏控制台约增 25 MB（09:54 至 12:31 增 148 MB），重启后仍会缓慢增长；根治是把系统默认终端改回“Windows 控制台主机”，属维护者的系统设置。

## 7. 教训

- 随时间线性增长的输入（活动段头、历史 manifest）必须有有界化步骤，且该步骤要在生产实际运行的那份代码里；主仓有而运维副本没有，等于没有（与 09-13 零行段头同源，第二次出现）。
- 写锁只应包住写入；把全量扫描包在单写者锁里，平时无害，缓存一冷就成为全局瓶颈。
- 任务计划程序的时限不是进程树的时限；需要有界运行时，由包装脚本自己计时并整树终止，同时保证留下完成记录。
- 链路健康要看耗时趋势而不只是失败次数：中位耗时自 09-09 起每天增加 1 至 2 分钟，到 09-17 才出现失败。
